# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
on:
  push:
    branches:
      - main
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    env: 
      VUE_FIREBASE_API_KEY: ${{secrets.VUE_FIREBASE_API_KEY}}
      # VUE_FIREBASE_AUTH_DOMAIN: ${{secrets.VUE_FIREBASE_AUTH_DOMAIN}}
      # VUE_FIREBASE_PROJECTID: ${{secrets.VUE_FIREBASE_PROJECTID}}
      # VUE_FIREBAE_STORAGEB: ${{secrets.VUE_FIREBAE_STORAGEB}}
      # VUE_FIREBAE_MESSAGIN: ${{secrets.VUE_FIREBAE_MESSAGEIN}}
      # VUE_FIREBASE_APPI_ID: ${{secrets.VUE_FIREBASE_APPI_ID}}
    steps:
      - uses: actions/checkout@v4
      - run: npm install
      - run: npm run build
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_DESAFIOAUTENTIFICACION_D67E0 }}
          channelId: live
          projectId: desafioautentificacion-d67e0
